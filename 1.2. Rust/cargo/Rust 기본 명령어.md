```bash
rustup update # rust를 업데이트 한다.
cargo build # 프로젝트를 컴파일하고 바이너리를 빌드한다. 기본은 debug 모드로 빌드된다.
cargo build --release # 릴리즈 버전으로 바이너리를 빌드한다. 실행속도가 최적화된다. 컴파일 속도가 조금 더 걸린다고 한다.
cargo run # 프로젝트를 컴파일하고 바이너리를 빌드한 후 바이너리를 실행한다.
cargo check # 프로젝트를 컴파일만 하고 바이너리를 빌드하지 않는다. 컴파일 에러를 체크하기 위해 사용한다.
```
